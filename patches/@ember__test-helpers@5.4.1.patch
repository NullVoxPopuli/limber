diff --git a/dist/-internal/debug-info.js b/dist/-internal/debug-info.js
index 45733a399fc957c2b01109ce9fb37172dfd7d721..ae723167fdbd88e189c47edb9d9a0a3c9d69e5d5 100644
--- a/dist/-internal/debug-info.js
+++ b/dist/-internal/debug-info.js
@@ -1,4 +1,3 @@
-import { _backburner } from '@ember/runloop';
 import { debugInfoHelpers } from './debug-info-helpers.js';
 import { getPendingWaiterState } from '@ember/test-waiters';
 
@@ -22,7 +21,7 @@ const SCHEDULED_AUTORUN = 'Scheduled autorun';
  * @returns {boolean} True if `getDebugInfo` is present in backburner, otherwise false.
  */
 function backburnerDebugInfoAvailable() {
-  return typeof _backburner.getDebugInfo === 'function';
+  return false;
 }
 
 /**
@@ -33,7 +32,7 @@ function backburnerDebugInfoAvailable() {
  * @returns {MaybeDebugInfo | null} Backburner debugInfo or, if the getDebugInfo method is not present, null
  */
 function getDebugInfo() {
-  return _backburner.DEBUG === true && backburnerDebugInfoAvailable() ? _backburner.getDebugInfo() : null;
+  return null;
 }
 
 /**
diff --git a/dist/setup-context-BSrEM03X.js b/dist/setup-context-BSrEM03X.js
index 0f688234691707c6ee3a1b0326efdd0edb1ea5cb..f406633b247c60925088b14db67ce6e87f0a93ae 100644
--- a/dist/setup-context-BSrEM03X.js
+++ b/dist/setup-context-BSrEM03X.js
@@ -1,4 +1,5 @@
-import { _backburner, run } from '@ember/runloop';
+import { run } from '@ember/runloop';
+import { _onRenderSettledChange as _emberOnRenderSettledChange, renderSettled as _emberRenderSettled } from '@ember/renderer';
 import { set, setProperties, get, getProperties } from '@ember/object';
 import { setOwner } from '@ember/application';
 import buildOwner from './build-owner.js';
@@ -16,7 +17,23 @@ import { getDeprecationsForContext, getDeprecationsDuringCallbackForContext } fr
 import { getWarningsForContext, getWarningsDuringCallbackForContext } from './-internal/warnings.js';
 import hasEmberVersion from './has-ember-version.js';
 import { runHooks } from './helper-hooks.js';
-import { hasPendingWaiters } from '@ember/test-waiters';
+import { buildWaiter, hasPendingWaiters, waitersSettled } from '@ember/test-waiters';
+
+// Rendering reports its pending/complete edges; bridging them into a
+// test waiter folds rendering into the same settledness protocol as
+// every other async source, so isSettled needs no render clause and a
+// stuck render is named in test-waiter debug output.
+const _emberRenderWaiter = buildWaiter('@ember/test-helpers:render');
+let _emberRenderWaiterToken = null;
+_emberOnRenderSettledChange((pending) => {
+  if (pending) {
+    _emberRenderWaiterToken ??= _emberRenderWaiter.beginAsync();
+  } else if (_emberRenderWaiterToken !== null) {
+    const token = _emberRenderWaiterToken;
+    _emberRenderWaiterToken = null;
+    _emberRenderWaiter.endAsync(token);
+  }
+});
 import { TestDebugInfo } from './-internal/debug-info.js';
 
 const CAN_USE_ROUTER_EVENTS = hasEmberVersion(3, 6);
@@ -330,15 +347,16 @@ function _setupAJAXHooks() {
   @returns {Object} object with properties for each of the metrics used to determine settledness
 */
 function getSettledState() {
-  const hasPendingTimers = _backburner.hasTimers();
-  const hasRunLoop = Boolean(_backburner.currentInstance);
+  const hasPendingTimers = false;
+  const hasRunLoop = false;
   const hasPendingLegacyWaiters = checkWaiters();
   const hasPendingTestWaiters = hasPendingWaiters();
   const pendingRequestCount = pendingRequests();
   const hasPendingRequests = pendingRequestCount > 0;
   // TODO: Ideally we'd have a function in Ember itself that can synchronously identify whether
   // or not there are any pending render operations, but this will have to suffice for now
-  const isRenderPending = !!hasRunLoop;
+  // counted via the render waiter above; a second clause would double-count
+  const isRenderPending = false;
   return {
     hasPendingTimers,
     hasRunLoop,
@@ -390,10 +408,31 @@ function isSettled() {
   @public
   @returns {Promise<void>} resolves when settled
 */
-function settled() {
-  return waitUntil(isSettled, {
-    timeout: Infinity
-  }).then(() => {});
+async function settled() {
+  // Settledness is awaited, not polled: rendering resolves
+  // renderSettled() when it completes, waiters resolve waitersSettled()
+  // on the endAsync that empties the last one. The fallback tick covers
+  // only sources that cannot announce completion (pull-only Waiter
+  // implementations, legacy waiters, request counters).
+  //
+  // Quiet is confirmed FROM A MACROTASK: already-queued task sources
+  // (worker messages, zero-delay timers) may register waiters or dirty
+  // state, and a microtask-context observation would win the race
+  // against them and settle early. And the loop re-checks, because
+  // finishing pending work can start more of it.
+  for (;;) {
+    // 50ms, not 10: the fallback has to LOSE to a frame-paced render
+    // tick, or it decides the race and costs an extra pass (measured
+    // 30 of 117 iterations at 10ms; 1 of 92 at 50ms).
+    await Promise.race([
+      Promise.all([_emberRenderSettled(), waitersSettled()]),
+      new Promise(resolve => setTimeout(resolve, 50)),
+    ]);
+    await new Promise(resolve => setTimeout(resolve, 0));
+    if (isSettled()) {
+      return;
+    }
+  }
 }
 
 const cachedOnerror = new Map();
@@ -777,7 +816,6 @@ function setupContext(base, options = {}) {
   setContext(context);
   const testMetadata = getTestMetadata(context);
   testMetadata.setupTypes.push('setupContext');
-  _backburner.DEBUG = true;
   _prepareOnerror(context);
   return Promise.resolve().then(() => {
     const application = getApplication();
