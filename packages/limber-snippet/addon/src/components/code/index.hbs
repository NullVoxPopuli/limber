{{#if this.isPreview}}

  <pre><code></code></pre>

{{else}}

    <iframe
      title={{this.title}}
      src="{{this.host}}?{{this.queryParams}}"
      style={{this.htmlSafe (concat "height: calc(1.5rem * " this.lines ");")}}
      ...attributes
    >
    </iframe>

{{/if}}