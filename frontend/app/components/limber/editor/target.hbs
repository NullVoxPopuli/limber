{{#let (service 'editor') as |context|}}
  <div
    {{@editor
      context.text
      context.updateText
      setValue=context.swapText
    }}
    class="
      border border-gray-900 overflow-hidden
    "
    ...attributes
  >{{context.text}}</div>
{{/let}}

