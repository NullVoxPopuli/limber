{{#let (service 'editor') as |context|}}

  {{#if context.markdownToHbs}}
    <pre class="htmlbars p-4" {{highlighted context.markdownToHbs}}><code class="htmlbars">{{context.markdownToHbs}}</code></pre>
  {{/if}}

{{/let}}
