{{page-title "Limber » Ember Markdown Playground"}}

<main class="grid grid-flow-col h-screen max-h-screen grid grid-rows-editor">
  <Limber::Header />

  {{!-- template-lint-disable no-inline-styles --}}
  <div class="grid overflow-hidden" style="grid-template-columns: 40% 8px 1fr;">
    <Limber::Editor />

    <div {{resize}} class="bg-ember-light-blue cursor-move"></div>

    <section class="w-full shadow-inner grid" style="grid-template-rows: min-content 1fr;">
      <Limber::Navigation />

      <div class="overflow-auto relative" {{constrain-vertically}}>
        {{outlet}}
      </div>

      <Limber::Error />
    </section>
  </div>

</main>